# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files & secrets (never commit)
.env
*.pem
service-account-key.json

# Firebase: commit firebase.json, .firebaserc, firestore.rules, firestore.indexes.json
# so the team can run: firebase.cmd deploy --only firestore:indexes (or rules)

.config/
scripts
SESSION_MEMORY.md

# AOS browser E2E artifacts
.aos-e2e/
playwright-report/
test-results/